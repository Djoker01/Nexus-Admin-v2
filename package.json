{
  "name": "nexus-admin",
  "version": "1.0.0",
  "description": "Nexus Admin - Sistema de Administración para Negocios",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Build completado'",
    "cap:init": "npx cap init 'Nexus Admin' 'com.tunegocio.nexusadmin' --web-dir=www",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "cap:build": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.0",
    "@capacitor/core": "^5.7.0",
    "@capacitor/cli": "^5.7.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.0"
  },
  "author": "",
  "license": "MIT"
}
